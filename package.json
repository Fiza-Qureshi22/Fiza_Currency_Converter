{
  "name": "fiza_currency_converter",
  "version": "1.0.0",
  "description": "A CLI-based currency converter application that allows users to convert amounts between different currencies using real-time exchange rates.",
  "main": "index.js",
  "type": "module",
  
  "publishConfig": {"access": "public"},
  "bin": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Fiza",
  "license": "MIT",
  "dependencies": {
    "inquirer": "^9.2.19"
  },
  "devDependencies": {
    "@types/inquirer": "^9.0.7"
  }
}

